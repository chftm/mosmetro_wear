import 'package:flutter/material.dart';
import 'package:mosmetro_wear/view/app.dart';

void main() {
  // The return value of this is irrelevant.
  // ignore: avoid-ignoring-return-values
  WidgetsFlutterBinding.ensureInitialized();

  runApp(const App());
}
